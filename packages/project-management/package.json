{
  "name": "@critical-claude/project-management",
  "version": "1.0.0",
  "description": "Project planning, timeline estimation, and initialization service",
  "main": "dist/server.js",
  "bin": {
    "critical-claude-project-management": "dist/server.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/server.js",
    "clean": "rm -rf dist",
    "test": "jest --passWithNoTests",
    "lint": "eslint src/**/*.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@critical-claude/core": "file:../core",
    "@modelcontextprotocol/sdk": "^0.5.0",
    "@iarna/toml": "^2.2.5"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0"
  },
  "keywords": [
    "mcp",
    "project-planning",
    "timeline-estimation",
    "initialization"
  ]
}